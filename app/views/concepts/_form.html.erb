<%= form_for(@concept) do |f| %>
  <% if @concept.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@concept.errors.count, "error") %> prohibited this concept from being saved:</h2>

      <ul>
      <% @concept.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :infraccion %><br>
    <%= f.text_area :infraccion %>
  </div>
  <div class="field">
    <%= f.label :monto %><br>
    <%= f.text_area :monto %>
  </div>
  <div class="field">
    <%= f.label :corralon %><br>
    <%= f.text_area :corralon %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
